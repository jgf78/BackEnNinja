banner:
  location:
          classpath:/folderExample/banner.txt
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/backendninja?useSSL=false
    username: root
    password: anliju78+
  thymeleaf:
    cache: false  
 
  jpa:
    show-sql: true  
    hibernate:
      ddl-auto: update
      naming:
        strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect                  